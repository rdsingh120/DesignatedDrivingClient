/**
 * Driver dashboard local style re-exports.
 * All values originate from the global theme — import from there directly
 * for new code, or use these named exports for backward compatibility.
 */
export { cardStyle, sectionLabel, TRIP_STATUS_INFO } from "../../../styles/theme";
